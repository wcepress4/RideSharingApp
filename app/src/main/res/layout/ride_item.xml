<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    android:elevation="8dp"
    android:radius="16dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="#FFFFFF">

        <!-- Settings (Gear) Icon - Delete Button -->
        <ImageButton
            android:id="@+id/settingsButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:background="@drawable/gear"
            android:backgroundTint="@android:color/holo_red_dark"
            android:contentDescription="Delete"
            android:padding="8dp"/>

        <LinearLayout
            android:id="@+id/cardContentLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="8dp">

            <!-- WHEN Title -->
            <TextView
                android:id="@+id/whenTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="WHEN"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="#888888"
                android:layout_marginBottom="4dp" />

            <!-- Date and Time on one line -->
            <TextView
                android:id="@+id/whenDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="April 23, 2025 · 5:00 PM"
                android:textSize="16sp"
                android:textColor="#000000"
                android:layout_marginBottom="16dp" />

            <!-- WHERE Title -->
            <TextView
                android:id="@+id/whereTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="WHERE"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="#888888"
                android:layout_marginBottom="4dp" />

            <!-- From ➔ To -->
            <TextView
                android:id="@+id/whereDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="From Location ➔ To Location"
                android:textSize="16sp"
                android:textColor="#000000"
                android:layout_marginBottom="16dp" />

            <!-- WITH Title -->
            <TextView
                android:id="@+id/withTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="WITH"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="#888888"
                android:layout_marginBottom="4dp" />

            <!-- Rider Name -->
            <TextView
                android:id="@+id/withDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Rider Name"
                android:textSize="16sp"
                android:textColor="#000000"
                android:layout_marginBottom="24dp" />

            <!-- Add/Edit Button (Single button that changes text dynamically) -->
            <Button
                android:id="@+id/addEditButton"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="Accept Ride"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:backgroundTint="@android:color/holo_red_dark"
                android:layout_gravity="center"/>
        </LinearLayout>
    </RelativeLayout>
</androidx.cardview.widget.CardView>
