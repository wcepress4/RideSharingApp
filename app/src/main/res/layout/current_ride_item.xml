<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#FFFFFF"
    android:elevation="4dp"
    android:layout_margin="8dp"
    android:backgroundTint="#F5F5F5"
    android:clipToPadding="false"
    android:clipChildren="false">

    <TextView
        android:id="@+id/whenDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date · Time"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="#000000" />

    <TextView
        android:id="@+id/whereDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="From → To"
        android:textSize="14sp"
        android:textColor="#000000" />

    <TextView
        android:id="@+id/withDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="With: User Name"
        android:textSize="14sp"
        android:textColor="#000000" />

    <TextView
        android:id="@+id/acceptedDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Waiting for Confirmation (0/2)"
        android:textSize="14sp"
        android:textStyle="italic"
        android:paddingTop="4dp"
        android:textColor="#444444" />

    <Button
        android:id="@+id/confirmRideButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mark as Complete"
        android:textColor="@android:color/white"
        android:backgroundTint="@android:color/holo_red_dark"
        android:layout_marginTop="8dp" />
</LinearLayout>
